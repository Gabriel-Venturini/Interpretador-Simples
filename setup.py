# this file setup the packages of the app

from setuptools import setup, find_packages

setup(name='BasicInterpreter', version='1.0', packages=find_packages())

# run this on terminal: pip install -e .